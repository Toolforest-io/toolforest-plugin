/**
 * Toolkit cache — no longer needed in meta-tool architecture.
 * Retained as a stub for backwards compatibility with any external imports.
 */

export class ToolkitCache {
  // Intentionally empty — toolkit discovery is now done on-demand
  // via the list_toolkits meta-tool rather than cached at startup.
}
